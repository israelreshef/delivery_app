# עבודה בשביל ישראל — מדריך אימות אבטחה

> [!IMPORTANT]
> מדריך זה מיועד למפעיל האנושי (ישראל) לאימות רכיבים שלא ניתן לבדוק בצורה אוטומטית לחלוטין.
> כל סעיף חייב להיבדק ולהיות מסומן כעובר (V) לפני ה-Go-Live.

## 🏛️ חלק 1 — אימות Base Infrastructure (Phase 1)

### 1.1 אימות HSM (Hardware Security Module)
**המטרה:** לוודא שהמפתחות נשמרים בחומרה ייעודית ולא בסימולציית תוכנה.

**פעולה:**
1. היכנס ל-AWS Console ← שירות **CloudHSM**.
2. ודא שה-Cluster מופיע במצב **ACTIVE**.
3. ודא שיש לפחות HSM Instance אחד במצב **ACTIVE**.
4. הרץ במסוף:
   ```powershell
   aws cloudhsmv2 describe-clusters
   ```
5. **תוצאה צפויה:** בשדות ה-JSON ודא ש-`State` הוא `ACTIVE` ושסוג המכשיר אינו "simulator".

---

### 1.2 אימות SPIFFE/SPIRE (זהות שירותים)
**המטרה:** לוודא שכל microservice קיבל תעודת זהות קריפטוגרפית.

**פעולה:**
1. הרץ בשרת ה-Production:
   ```bash
   /opt/spire/bin/spire-server agent list
   ```
2. ודא שכל ה-Agents מופיעים במצב `attested`.
3. ודא שזמן התוקף (`Expires`) קטן מ-24 שעות (מעיד על רוטציה תקינה).
4. **בדיקת עומס mTLS:** הרץ את כלי ה-Load Test הפנימי עם 500 חיבורים מקבילים וודא שה-Overhead הוא פחות מ-2ms.

---

### 1.3 אימות Vendor DPAs
**המטרה:** לוודא חוקיות העברת מידע PII לספקים (תיקון 13).

**פעולה:**
1. פתח את הקובץ: [vendor_dpa_registry.md](file:///c:/Users/Israel/Desktop/delivery_app/docs/vendor_dpa_registry.md)
2. ודא שלכל ספקי ה-PII (AWS, Stripe, וכו') יש תאריך חתימה עדכני.
3. ודא שבתיקיית `/legal/dpas/` קיימים עותקים סרוקים של כל ההסכמים.
4. **דגש:** אין להפעיל תעבורת נתונים לספק שאין לגביו סטטוס ✅ [ACTIVE].

---

## 🔐 חלק 2 — אימות מנגנוני אבטחה (Phase 2)

### 2.1 מעבר WAF מ-COUNT ל-BLOCK
**המטרה:** הפעלת הגנה אקטיבית לאחר 48 שעות של למידה.

**פעולה:**
1. בתום 48 שעות מההפצה, היכנס ל-AWS Console ← **WAF & Shield**.
2. נווט ל-Web ACL של הפרויקט.
3. שנה את ה-Rules מ-`COUNT` ל-`BLOCK`.
4. **בדיקה:** נסה לשלוח בקשה זדונית מנקודת קצה חיצונית:
   ```bash
   curl -X GET "https://api.tzir-delivery.co.il/orders?id=1' OR '1'='1"
   ```
5. **תוצאה צפויה:** קבלת שגיאה `403 Forbidden` מה-WAF.

---

### 2.2 אימות Audit Log Dual-Write
**המטרה:** לוודא שלוגים לא נאבדים בנפילת תשתית.

**פעולה (בסביבת Staging):**
1. הפסק את שירות ה-Redis: `sudo systemctl stop redis`.
2. בצע ניסיון התחברות (Login) לאפליקציה.
3. בדוק את הקובץ המקומי בשרת: `tail -f logs/audit.log`.
4. **תוצאה צפויה:** אירוע ה-Login נרשם בקובץ המקומי למרות ש-Redis לא זמין.
5. הפעל את ה-Redis: `sudo systemctl start redis`.
6. ודא שה-Reconciliation Job העביר את הנתונים ל-S3 ושרשרת ה-HMAC נשמרה תקינה.

---

## 🏢 חלק 3 — מדיניות אופרטיבית (Phase 3)

### 3.1 אימות JIT (Just-In-Time) Access
**המטרה:** מניעת גישה קבועה לפרודקשן ואכיפת אישור כפול.

**פעולה:**
1. היכנס למערכת הניהול כמשתמש A.
2. בקש גישה ל-4 שעות (JIT).
3. נסה **לאשר לעצמך** את הבקשה.
4. **תוצאה צפויה:** קבלת הודעת שגיאה "Self-approval is strictly prohibited".
5. ודא שלאחר 4 שעות בדיוק הגישה נחסמת אוטומטית.

---

### 3.2 אימות Offboarding מהיר
**המטרה:** ביטול גישה מלא תוך פחות מ-60 דקות.

**פעולה:**
1. בעת עזיבת עובד, הרץ את הסקריפט:
   ```powershell
   python scripts/security/offboarding_checklist.py --user [employee_id]
   ```
2. בדוק את הקובץ `revocations_report.json`.
3. ודא שכל המערכות (GitHub, AWS, Vault, VPN) מסומנות כ-`REVOKED`.

---

## 📱 חלק 4 — אבטחת מובייל (מכשיר פיזי בלבד)

### 4.1 אימות TEE (Hardware Keystore)
**פעולה:**
1. הפעל את האפליקציה על מכשיר מובייל (לא אמולטור!).
2. חבר את המכשיר למחשב והרץ:
   ```bash
   adb shell dumpsys keystore2
   ```
3. חפש את ה-Alias: `tzir_delivery_master_key`.
4. **תוצאה צפויה:** ודא ש-`SecurityLevel` הוא `STRONGBOX` או `TRUSTED_ENVIRONMENT`.

---

### 4.2 אימות FLAG_SECURE
**פעולה:**
1. נווט למסך ה-Login או למסך פרטי הזמנה.
2. נסה לצלם Screenshot.
3. **תוצאה צפויה:** המכשיר יחסום את הצילום או שהקובץ יצא שחור.

---

## 🏆 סיכום ובדיקת דרוך (Go-Live)
לפני הלחיצה על כפתור ההפצה הסופי, ודא שכל התיבות הבאות מסומנות ב-V:

- [ ] כל בדיקות ה-Automated Security בסביבת Staging עברו בהצלחה.
- [ ] ציון **A+** בכל שלושת אתרי הבדיקה (SecurityHeaders, Mozilla, SSL Labs).
- [ ] סימולציית Phishing ראשונה בוצעה והעובדים הרלוונטיים הוכשרו.
- [ ] ה-DR Plan נוסה (Restore Test) וזמן השחזור עמד ביעד ה-RTO.
- [ ] כל ה-DPAs חתומים פיזית וסרוקים.
