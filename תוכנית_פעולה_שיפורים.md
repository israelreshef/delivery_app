# תוכנית פעולה — שיפורים באפליקציית השליח והאתר (TZIR)

> גרסה: 2026-08-04 (v4 — בוצעו: סגירת Phase A, עדכון סטטוסים לפי בדיקה בפועל) · סטטוס: **בביצוע (Phase A הושלם)**
> היקף: אפליקציית Android (`mobile-native/courier-android`) + אתר Next.js (`frontend`) + Backend (`backend`)

---

## סקירה כללית

| # | אזור | עוצמת עבודה | סדר מומלץ | סטטוס |
|---|------|-------------|-----------|-------|
| 0 | **אבטחה, ציות, תפעול וממשל נתונים (בלוקר לפרודקשן)** | **גדול, מתמשך** | **0 — במקביל להכל** | ⏳ במעקב (S1 HTTPS/Pinning ✅, KSP ✅) |
| 1 | ניהול עסקי — הפרשות פנסיה + קרן השתלמות | קטן | A (4) | ✅ בוצע (קיים מקצה לקצה) |
| 2 | עמוד בקרה (תכנון מסלולים) — התאמת צבע למותג | קטן-בינוני | A (1) | ✅ בוצע (כבר על המותג) |
| 3 | יומן — יצירת משלוח בלחיצה על שעה (סגנון Google) | בינוני | B (2) | ✅ בוצע (קיים מקצה לקצה) |
| 4 | הגדרות — תואר רמת השליח (לא "פריימיום") | קטן | A (3) | ✅ בוצע ("שליח מתחיל") |
| 5 | הגדרות — מצב יום/לילה מותאם + סוללה וביצועים | בינוני | C (3) | ✅ בוצע (Theme גלובלי + הסרת hardcoded בכל המסכים) |
| 6 | הגדרות — תמונת פרופיל / לוגו (הוספה והסרה) | בינוני | C (5) | ✅ בוצע (Photo Picker + העלאה) |
| 7 | טפסים ודוחות מס אוטומטיים (מסמכים) | גדול | D (6) | ✅ בוצע (סבבים 1–3: Catalog+Generate+Blank+UI 전체+אתר; נותר היסטוריית דוחות אופציונלית/מאוחרת) |
| E | הקשחה תפעולית: Zero Trust, Feature Flags, DR, Chaos | בינוני-גדול | E | ⏳ פתוח |

**עקרון-על של הפרויקט:** "לחסוך לשליח את כל הטרחה והפרוצדורות" → כל דוח נבנה אוטומטית מנתוני המערכת, והשליח רק מוריד ומגיש.
**עקרון-על שני (בלוקר):** שום דבר לא עולה לפרודקשן בלי שסעיפי האבטחה/ציות (S1–S8, R1–R7) וצ'ק ליסט "Definition of Done" אושרו.

---

## 0. דוח סקירת אינטגרציה מאומתת (2026-08-02)

> שיטה: מעקב חיבור server→app→UI, בדיקת רישום blueprints ב-`app.py`, ובדיקת שימוש אמיתי בקוד. תכלית: **לא לעבוד כפול** — רק מה ש"יושב כקובץ מת" נבנה; מה שרץ מקצה-לקצה מקבל וי ומשודרג בלבד.

### קבצים מתים (DEAD CODE — קיימים אבל לא מחוברים) ❌
- `backend/middleware/api_security.py` (`security_middleware` — HMAC-SHA256 + Replay + Threat-Intel + Anomaly + Schema Validation): **מוגדר אבל לא מופעל על אף route**. ה-HMAC key (`dev-device-hmac-key`) קבוע בקוד, וה-App לא שולח X-Signature בכלל.
- `backend/routes/data_rights.py`: blueprint **לא רשום** ב-`app.py` (הפונקציונליות קיימת ברישום `privacy.py`).
- `mobile-native/.../security/SecurityManager.kt`: **לא מוזכר בשום מקום ב-src** (הפונקציונליות החיה: `utils/SecureStorage.kt` + `security/SecurityEnforcer.kt`).

### מה כן מחובר ומאומת (WIRED) ✅
- **ניהול עסקי (#1):** ✅ Mobile → `/api/courier/business/*` (overview, reports/monthly, reports/annual, expenses, receipts, receipts/<id>/document) — הכל קיים ב-`courier_business.py` ורשום. הפער היחיד: שדות פנסיה/השתלמות.
- **יומן (#3):** ⚠️ `GET /api/courier/schedule` קיים ומחובר (`getMySchedule`), מחזיר כבר pickup/dropoff. **חסר:** POST ליצירה + UI בלחיצת שעה.
- **מסמכים (#7):** ✅ `/api/couriers/documents` קיים ומחובר — מחזיר מסמכים שהועלו (`CourierDocument`). **חסר:** תבניות טפסי מס + גנרטור אוטומטי.
- **אימות + טוקנים:** ✅ Mobile משתמש ב-`SecureStorage`(AndroidKeyStore) + `TokenManager` + refresh אוטומטי ב-`KtorClientFactory` (Bearer). Access = 60 דק' (לא 15), refresh = 180 יום.
- **Audit log (G2):** ✅ `log_audit` פעיל ונקרא ב-25+ מקומות (auth/orders/finances/freelance/privacy/hr...) — כותב ל-DB (`AuditLog`) + JSON (`logs/security.json`). **לא WORM** (ניתן לעריכה/מחיקה ב-DB).
- **WebSocket:** ✅ `init_sockets` רשום (delivery/chat/support events) + test קיים.
- **RLS (Postgres):** ✅ `before_request` מזריק `app.current_user_id`/`app.is_admin` — פעיל בסביבת Postgres.
- **Rate limiting (S3):** ⚠️ מופעל רק על auth/addresses/orders — לא גלובלי.
- **MFA (S2):** ⚠️ Backend+אתר: `/auth/2fa/setup|verify-and-enable|login-verify` + WebAuthn (אתר בלבד). נאכף **רק** לאדמין עם `mfa_enabled` (opt-in). **Mobile: אין MFA בכניסה** (`AuthRepository.login` = username/password בלבד). (ה-OTP ב-App הוא אימות מסירה, לא כניסה.)
- **Privacy/PII (S5):** ⚠️ `/api/privacy/export|account|consent` רשום ונבדק (`test_privacy_compliance`). **Mobile: אין מסך/טיפול consent**.
- **SecurityEnforcer (S6):** ✅ רץ ב-`MainActivity` (מינ. גרסה + Emulator detection + Hardware-backed). ⚠️ **אין Root/Jailbreak detection**; ב-Emulator עוקף בדיקת חומרה.
- **Frontend Dashboard:** ✅ קורא `/couriers/available-orders|active-order|stats|orders/{id}/accept|reject|status` — כולם קיימים ב-`couriers.py`.
- **ארנק/תשלומים (R6):** ⚠️ `courier_wallet.py` + methods ב-Mobile מחוברים (balance/history/withdrawals). **PSP/Stripe חיצוני — לא מאומת** (לפי `PROJECT_STATUS` שילוב תשלומים נותר בלוקר).

### פער קריטי — TLS (S1)
- ❌ `KtorClientFactory.kt:34` — base URL הוא **HTTP** (`http://10.0.2.2:5000` באמולטור / `http://192.168.33.18:5000` במכשיר) — **אין HTTPS ואין Pinning מהאפליקציה**.

### בדיקות קיימות (tests) ✅
- `test_api_smoke` (security headers + CORS), `test_privacy_compliance`, `test_auth_integration`, `test_websocket_integration`, `test_payments_integration`, `test_invoice_concurrency`, `test_zone_allocation`.
- ❌ אין tests ל-MFA, Rate-limit, Audit-log, HMAC/middleware, RLS — להוסיף בשלב 0.

### כיצד להשתמש בדוח זה
- כל פריט עם ✅ = **לא לבנות מחדש** — רק לבדוק/לשדרג.
- כל פריט עם ❌ (קובץ מת / לא קיים) = מועמד לבנייה, ומצוין בסעיפים 8–12 להלן.

---



## 1. מסך ניהול עסקי — הפרשות לפנסיה ולקרן השתלמות

> **סטטוס: ✅ בוצע (אומת 2026-08-04)** — Backend + Model + UI כולם קיימים ומחוברים.

**מצב קיים:**
- `mobile-native/.../ui/courier/BusinessScreen.kt` → `ReportsView` מציג 2 כרטיסים: **דוח חודשי** ו-**סיכום שנתי**.
- Backend: `backend/routes/courier_business.py` → `monthly_report` ו-`annual_report` מחשבים הכנסות/הוצאות/מע"מ/רווח; קיים כבר `social_security_estimate` (ביטוח לאומי) בדוח השנתי.
- Model: `mobile-native/.../model/BusinessModels.kt` → `MonthlyReport`, `AnnualReport`.

**צ'ק ליסט:**
- [x] Backend — הוספת חישוב ב-`annual_report` (ו/או `monthly_report`): `pension_contribution` (הפרשות לפנסיה) ו-`study_fund_contribution` (הפרשות לקרן השתלמות) לפי אחוז קבוע מהרווח/ההכנסה (יש להגדיר עם בעל העסק; הערכה התחלתית: פנסיה 6% + קרן השתלמות 6% ממשכורת עצמית/רווח). — **קיים: `PENSION_RATE = 0.06`, `STUDY_FUND_RATE = 0.06` (`courier_business.py:18-19`), מוחזרים ב-`monthly_report:440-441` וב-`annual_report:488-489`.**
- [x] Model — הוספת השדות ל-`AnnualReport` / `MonthlyReport` ב-`BusinessModels.kt`. — **קיים: `pensionContribution` + `studyFundContribution` (שורות 67-68, 81-82).**
- [x] UI — הוספת שורה/שורות בתחתית הכרטיסים ב-`ReportsView`: "הפרשות לפנסיה" ו"הפרשות לקרן השתלמות". — **קיים: `BusinessScreen.kt:449-450` (חודשי), `466-467` (שנתי).**
- [ ] עדכון `DeliveryApi.kt` (ה-response של `getAnnualReport`/`getMonthlyReport`) במידה ויש deserialization מפורש. — **לא נדרש (serialization לפי `@SerialName`).**

**דרך ביצוע:** ✅ הושלם מקצה לקצה.

---

## 2. עמוד בקרה (תכנון מסלולים) — התאמת צבע למותג

> **סטטוס: ✅ בוצע (אומת 2026-08-04)** — הפלטות כבר על המותג (Navy/Blue) בשני המסכים; בוצעה בדיקה ויזואלית בקוד.

**מצב קיים:**
- `RoutePlannerScreen.kt` — פלטת ה-"Obsidian Mist" כבר **מיושרת למותג** דרך ה-aliases ב-`Color.kt`: `ObsidianBg=BackgroundDark`, `ObsidianSurface=SurfaceDark`, `ObsidianElevated=Surface2Dark`, `ObsidianGlass=GlassDark`, `ObsidianBorder=BorderDark`, `SoftMint=Ice`, `SoftBlue=BrandBlueLight` (שורות 66-74). הגוון הירוק `0xFF8ECFB9` וה-`0xFFA0C4E8` אינם קיימים עוד.
- `ManualRoutePlannerScreen.kt` — עבר למותג: רקע `BackgroundDark` (שורה 177), accent `BrandBlue` + `BrandBlueLight` בכל המרכיבים; אין עוד `0xFF0E0E0E`/`0xFF4FC3F7`.

**צ'ק ליסט:**
- [x] החלפת פלטת `Obsidian*` + `SoftMint`/`SoftBlue` ב-`RoutePlannerScreen.kt` בפלטת המותג. — **קיים (aliases למותג).**
- [x] סנכרון `ManualRoutePlannerScreen.kt` לאותה פלטה. — **קיים.**
- [x] בדיקת ניגודיות: טקסט קריא על משטחי המותג. — **מאומת: `TextPrimaryDark=Ice` על `Navy950` (ניגודיות גבוהה), pickup=`BrandBlueLight` על `BrandBlueLight.copy(alpha=0.15f)`.** נותר בדיקה ויזואלית ב-Emulator.
- [ ] וידוא שכל 3 הכניסות (CONTROL / הגדרות / כפתור מסלול) מראות עיצוב אחיד. — **נדרש אישור ויזואלי ב-Emulator.**
- [ ] אופציונלי: טיפול ב-`RouteOptimizationScreen.kt` (צבעי success/error ירוק-אדום — תקין, רק לוודא התאמה).

**דרך ביצוא:** ✅ הפלטות מותאמות; נשאר אישור ויזואלי.

---

## 3. עמוד יומן — יצירת משלוח בלחיצה על שעה (סגנון Google)

> **סטטוס: ✅ בוצע (אומת 2026-08-04)** — מקצה לקצה: Backend POST + Model + Repository + UI Dialog.

**מצב קיים:**
- `CalendarScreen.kt` → `DayView` מציג ציר שעות 7–19; כל שורת שעה היא **clickable** (`CalendarScreen.kt:548`) ופותחת Dialog "הוספת משלוח בשעה HH:00".
- `AddScheduleDialog` (קובץ `CalendarScreen.kt:620`) מכיל: כותרת, **כתובת איסוף**, **כתובת יעד**, שעת סיום אופציונלית.
- הבלוק שנוצר מציג `dropoffAddress` (fallback ל-`address`) — `CalendarScreen.kt:581`.

**צ'ק ליסט:**
- [x] Backend — Endpoint `POST /api/courier/schedule` ליצירת ערך יומן. — **קיים (`courier_notifications.py:174` `create_schedule_entry`), מודל `CourierScheduleEntry` (שורה 199).**
- [x] Mobile Model — `CalendarDelivery`/`ScheduleDelivery` עם `pickupAddress` + `dropoffAddress`. — **קיים (למשל `CalendarRepository.kt:51-52`).**
- [x] `DeliveryApi.kt` — מתודת `createScheduleEntry(...)` + חשיפת pickup/dropoff ב-`getMySchedule`. — **קיים (`DeliveryApi.kt:94`, impl `848`).**
- [x] `CalendarRepository.kt` — מתודת create + רענון. — **קיים (`CalendarRepository.kt:70`).**
- [x] UI (`DayView`) — שורות שעה clickable → Dialog עם שעה/איסוף/מסירה/משך + שמירה. — **קיים (`CalendarScreen.kt:597-617`).**
- [x] הצגת pickup → dropoff בבלוק שנוצר. — **קיים (`dropoffAddress`, שורה 581).**

**דרך ביצוע:** ✅ הושלם מקצה לקצה.

---

## 4. הגדרות — תואר רמת השליח (לא "שליח פריימיום")

> **סטטוס: ✅ בוצע (2026-08-04)** — בחירה (א) פשוטה: תואר קבוע "שליח מתחיל" עד שהאקדמיה תהיה פעילה.

**מצב קיים:**
- `SettingsScreen.kt` (שורה 151): badge קבוע **"שליח פריימיום"** — מטעה, כי האקדמיה עדיין לא נבנתה והשליח לא עבר קורסים.
- רמת השליח קיימת אצל Gamification (`backend/routes/academy.py`/`academy_protocols.py`) אך לא נמשכת למסך הזה.

**צ'ק ליסט:**
- [x] החלטה על מקור הנתונים לתואר: **(א)** פשוט מיידי — "שליח מתחיל" (אקדמיית ציר עדיין לא פעילה). **(ב)** דינמי — נדחה לעתיד.
- [x] החלפת הטקסט ב-`SettingsScreen.kt` (ושורה 151: "שליח פריימיום" → **"שליח מתחיל"**). אומת: אין עוד מופע של "שליח פריימיום" במודול courier.
- [ ] אם (ב): להוסיף שדה ל-`User`/`Courier` בחזרה + מפה תואר-רמה. — **נדחה** (יתווסף עם האקדמיה).

**דרך ביצוע:** ✅ בוצע. בדיקת גרסאות טקסט RTL — לאורך כל המסכים.

---

## 5. הגדרות — מצב יום/לילה מותאם + ביצועים וסוללה

> **סטטוס: 🟠 ביצוע חלקי (2026-08-04)** — ליבת ה-theme (גלובלי + ראקטיבי + toggle) הושלמה; הסרת רקעים hardcoded מסך-מסך נשארה (שלב הדרגתי).

**מצב קיים:**
- Toggle "מצב לילה" קיים (`PREF_DARK_MODE`, כברירת מחדל `true`) ב-`SettingsScreen.kt`, ונשמר ב-SharedPreferences.
- **הבעיה:** כיבוי לא עבד כי `getDefaultTheme()` החזיר תמיד `true` — אין מצב יום בפועל.
- קיימים כבר במערכת: `TZIRLightColorScheme` (Paper/Ice/Navy) + `TZIRExtendedColors` כפול (Light/Dark) ב-`Theme.kt`.

**צ'ק ליסט:**
- [x] קריאת `PREF_DARK_MODE` בהשקת ה-App והעברת `darkTheme` ל-`MyApplicationTheme`. — **בוצע: `DarkModeState` (Theme.kt) + טעינת pref ב-`MainActivity.onCreate` + `MyApplicationTheme(darkTheme = DarkModeState.isDarkTheme)`.**
- [x] חיבור ה-toggle למצב אמיתי. — **בוצע: השינוי ב-Settings מעדכן את `DarkModeState.isDarkTheme` (ראקטיבי) + שומר pref — התימה מתחלפת מידית (עד לנקודת ההרכבה של Material).**
- [x] הסרת ה-hardcoded רקעים כהים מ-**`SettingsScreen.kt`** (מסך ראשי + SubSheet): רקע/כרטיס → `MaterialTheme.colorScheme.background/surface`, מפרידים → `outlineVariant`, טקסט ראשי → `onSurface/onBackground`, משני → `onSurfaceVariant`, avatars → `surfaceVariant`, אזור Academy → `BrandBlue.copy(alpha=0.08f)`. — **בוצע + אומת (`compileDebugKotlin` + `:courierApp:test` EXIT=0).**
- [x] הסרת ה-hardcoded רקעים כהים מ-**`CalendarScreen.kt`** (כל quattro התצוגות: Month/Week/Day/Year + דיאלוג עריכה + `DeliveryListCard`): רקע/כרטיס → `background/surface`, מפרידים → `outlineVariant`, טקסט → `onSurface/onSurfaceVariant`. — **בוצע + אומת (EXIT=0).**
- [x] הסרת ה-hardcoded טקסטים כהים מ-**`BusinessScreen.kt`** (`W→onSurface` ×18, `G→onSurfaceVariant` ×13; שמירת לבן על כפתור הכחול). — **בוצע + אומת (EXIT=0).**
- [x] הסרת ה-hardcoded טקסטים כהים מ-**`MoreScreen.kt`** (`W→onSurface` ×5, `G→onSurfaceVariant` ×6). — **בוצע + אומת (EXIT=0).**
- [x] הסרת ה-hardcoded טקסטים כהים מ-**`DocumentsScreen.kt`** (`G→onSurfaceVariant` ×6). — **בוצע + אומת (EXIT=0).**
- [x] הסרת ה-hardcoded טקסטים כהים מ-**`ProfileScreen.kt`** (`W→onSurface` ×5, `G→onSurfaceVariant` ×5; תוך שמירת `containerColor=White` הקיים — ללא רגרסיה). — **בוצע + אומת (EXIT=0).**
- [x] הסרת ה-hardcoded טקסטים/רקעים כהים מ-**כל יתר מסכי השליח** (ManualRoutePlanner, Missions, MissionDetails, MissionHistory, MissionProtocol, Earnings, Clients, ClientDetail, Balance, Expense, Academy, CourseDetail, Quiz, Leaderboard, NotificationCenter, PaymentMethods, RouteOptimization, Dashboard, Vehicle, WorkerRating, SupportChat, Login, Register) + קומפוננטות (ErrorState, TzirButton, PremiumComponents). — **בוצע + אומת (`compileDebugKotlin` + `:courierApp:test` EXIT=0).** שמירה מכוונת של לבן על כחול-מותג/כרטיסי צבע ומפרידים.
- [ ] **בדיקת מצב יום/לילה במכשיר** (החלפה חיה, ניגודיות) — לא בוצע עדיין.
- [ ] הגדרת **מצב יום** מותאם למותג (קיים): רקע `Paper`/`Ice`, טקסט `Navy950`, Accent `BrandBlue`, ניגודיות גבוהה (WCAG AA). — **מאומת בפועל בכל מסכי השליח; נותר אימות ויזואלי במכשיר.**
- [ ] הגדרת **מצב לילה** חסכוני בסוללה: שחור AMOLED `#000` על OLED + הגבלת אנימציות. — **אופציונלי/מאוחר.**
- [ ] בדיקת ביצועים: גרירת סוללה בין המצבים (מטרה: <10% ל-8 שעות). — **אופציונלי/מאוחר.**

**דרך ביצוא:** שלב 1 (חיבור theme גלובלי + toggle) ✅ הושלם ואומת. שלב 2 (הסרת hardcoded colors) — **הושלם בכל מסכי השליח** (Settings, Calendar, Business, More, Documents, Profile, וכל השאר + קומפוננטות) ואומת (EXIT=0); נותר אימות ויזואלי במכשיר.

---

## 6. הגדרות — תמונת פרופיל / לוגו (הוספה והסרה)

> **סטטוס: ✅ בוצע (2026-08-04)** — Photo Picker + העלאה לשרת + תצוגה + הסרה, ב-`SettingsScreen.kt`.

**מצב קיים:**
- האווטאר ב-`SettingsScreen.kt` הוא `Box` סטטי עם אייקון `Person` — אין אפשרות להוסיף/להסיר תמונה.
- Backend: `POST /api/couriers/upload` (`couriers.py:24`) מחזיר URL של קובץ.

**צ'ק ליסט:**
- [x] Backend — Endpoint העלאת תמונת פרופיל — **מנופת: `api.uploadImage()` קיים (`DeliveryApi.kt:276`) → `/api/couriers/upload`.**
- [x] Mobile — Photo Picker (Photo Picker API `ActivityResultContracts.PickVisualMedia`).
- [x] UI — לחיצה על האווטאר → תפריט: הוספת תמונה / ללא תמונה / ביטול; תצוגת התמונה (Coil `AsyncImage` + Crop) עם ה-URL המלא (`KtorClientFactory.resolveBaseUrl()` + נתיב יחסי).
- [x] עדכון גרסה: `courierRepository` הוזרק ל-`SettingsScreen` (CourierNavGraph); URL נשמר ב-SharedPreferences (`pref_profile_photo`).

> הערה: ה-`coil-compose` כבר היה תלות במודול (לא נוסף מחדש). אומת קומפילציה + `:courierApp:test` (BUILD SUCCESSFUL).

**דרך ביצוע:** ✅ Photo Picker מודרני + העלאה לשרת + ממשק בחירה פשוט.

---

## 7. טפסים ודוחות מס אוטומטיים (מסמכים)

**מצב קיים:**
- `DocumentsScreen.kt` מציג מסמכי שליח מה-API (`getDocuments`) + פריטי רגולציה סטטיים.
- Backend קיים חלקית: `GET /api/earnings/vat-summary` (מע"מ), `GET /api/earnings/annual-summary` (1301), `backend/routes/reports.py` (Excel עם מע"מ/מקדמות מס/ביטוח לאומי/דו"ח 856), `pdf_generator.py` (invoice, earnings, receipt).
- חסר: מנגנון "צור דוח" לכל טופס + טופסי PDF/Excel ריקים להורדה.

**צ'ק ליסט — רשימת הטפסים:**

| טופס | סוג | דרך ביצוע |
|------|-----|-----------|
| מע"מ חודשי (הכנסות מול הוצאות) | מחולל אוטומטי | ✅ `vat_monthly` (`courier_forms.py`) — נותרת מ `vat-summary`+Excel |
| מקדמות מס הכנסה | מחולל אוטומטי | ✅ `tax_advances` |
| מקדמות ביטוח לאומי | מחולל אוטומטי | ✅ `national_insurance` |
| דוח שנתי למס הכנסה 1301 | מחולל אוטומטי | ✅ `annual_1301` |
| טופס 106 — אישור ניכוי מס במקור | מחולל אוטומטי | ✅ `withholding_106` — מסוכם מ-`Expense.withholding_tax_deducted` (is_contractor_invoice) |
| הצהרת הון | טופס ריק + מילוי פרטים | ✅ `wealth_declaration` (תבנית ריקה + פרטי שליח) |
| שומת סוף שנה \| חישוב הפרשי ביטוח לאומי | מחולל אוטומטי | ✅ `year_end_assessment` — חישוב הפרשים מול מקדמות |
| טופס תיאום מס 116 | טופס ריק + מילוי פרטים | ✅ `tax_coordination_116` |
| אישור ניכוי מס במקור וניהול ספרים | מחולל אוטומטי | ✅ `withholding_bookkeeping` — ריכוז תשלומים/ניכויים (דמוי 856) |

**צ'ק ליסט — פונקציונליות כללית:**
- [x] Backend — מודל "תבניות טפסים" + רשימת טפסים לכל שליח (כבר יש `CourierDocument`/`get_documents` — להרחיב עם סוגי טפסים ו-status). (**קטלוג `utils/tax_forms.py` `FORMS` (6 טפסים) + `GET /api/courier/forms`.**)
- [x] Backend — endpoint `POST /api/courier/forms/<form_id>/generate` שממלא את הטופס מנתוני השליח (הכנסות, הוצאות, משלוחים, מסים, פרטים אישיים) ומחזיר קובץ PDF/Excel. (**`courier_forms.py` — מע"מ 18%, מקדמות 5%, ביטוח לאומי 9%, 1301 84120.**)
- [x] Backend — endpoint הורדת טופס **ריק** (PDF/Word) לכל טופס. (**`GET /api/courier/forms/<form_id>/blank`.**)
- [x] UI (`DocumentsScreen.kt`) — סקשן **"דוחות מס"** מעל הרשימה: כרטיס לכל טופס עם:
  - שם הדוח
  - כפתור **"צור דוח"** → המערכת ממלאת → הורדה (POD/DocumentOpener).
  - כפתור **"הורד טופס ריק"**.
  - סטטוס: "מוכן להגשה" / "לא זמין עדיין". (**`TaxFormCard` + `PeriodPickerDialog` + `DeliveryApi`/`CourierRepository` methods; פתיחת PDF דרך `DocumentOpener.openBytes`.**)
- [x] חיבור לעמוד השליח גם באתר: `frontend/app/courier/documents/page.tsx` — אותו מבנה (טפסים + צור דוח). (**`freelanceApi.getTaxForms/generateTaxForm/downloadBlankForm` + `TaxForm` types + סקשן "דוחות מס" עם PeriodPicker (month/year) והורדת PDF.**)
- [x] שמירת היסטוריית דוחות שנוצרו + סימון שצריך לרענן.

**דרך ביצוע (המלצה):** פיצול ל-3 סבבים — (1) מנגנון הורדת טופס ריק + "צור דוח" למה שכבר קיים ב-Backend (מע"מ, מקדמות, ביטוח, 1301); (2) טופס 106 + שומת סוף שנה + אישור ניכוי וניהול ספרים (מחוללים חדשים); (3) הצהרת הון + טופס 116 (תבניות ריקות + מילוי פרטים). בחירת תבניות PDF: `reportlab`/`fpdf` או תבניות Word ב-`docx_generator.py` (קיים).

> **סטטוס: ✅ בוצע (2026-08-05, כל 3 הסבבים).** הקטלוג `FORMS` כולל כעת 9 טפסים (`vat_monthly`, `tax_advances`, `national_insurance`, `annual_1301`, `wealth_declaration`, `tax_coordination_116`, `withholding_106`, `year_end_assessment`, `withholding_bookkeeping`). הושלם: Frontend `documents/page.tsx` (סקשן "דוחות מס" עם צור דוח + טופס ריק + PeriodPicker month/year והורדת PDF דרך `freelanceApi`), ו-tests Backend (`test_courier_forms.py`, 10 בדיקות).

> **שמירת היסטוריית דוחות — ✅ בוצע (2026-08-06, ראה `תוכנית_פעולה_השלמת_סעיף7.md`).** מודל `CourierReportHistory` (טבלה `courier_report_history`, UniqueConstraint ל-(courier, form, period) — upsert ללא כפילויות); migration `a1f2c3d4e5f6_add_courier_report_history`; שמירת PDF ב-`generate` עם Header `X-Report-Id` + `log_audit('report_generated')`; endpoints חדשים `GET /api/courier/forms/history` (עם status חי `up_to_date`/`needs_refresh` לפי `source_fingerprint`), `GET .../history/<id>/download` ו-`DELETE .../history/<id>` עם `log_audit('report_downloaded'/'report_deleted')` והגנת בעלות (BOLA/IDOR). UI: Frontend `documents/page.tsx` — סקשן "דוחות שנוצרו" (badge עדכני/דורש רענון + הורדה/מחיקה/רענון); Mobile `DocumentsScreen.kt` — סקשן "דוחות שנוצרו" זהה (סטטוס + הורדה/מחיקה/רענון, רענון אוטומטי אחרי generate). Blank forms ממולאים כעת בפרטי השליח (ת.ז., טלפון, אימייל, כתובת). תיעוד retention (7 שנים) ב-`docs/data_retention_policy.md`. Tests: `test_courier_forms.py` — 20 בדיקות (כולל BOLA ו-IDOR); חבילת Backend 88 passed. **הצפנת PDF במנוחה (2026-08-07, חובה):** `file_bytes` נשמר מוצפן AES-256-GCM ב-DB — `EncryptedLargeBinary` (TypeDecorator ב-`models.py`) + `encrypt_bytes`/`decrypt_bytes` ב-`utils/encryption.py`; הורדה מפענחת שקוף ומחזירה PDF תקין; tests `test_report_file_bytes_encrypted_at_rest` / `test_report_file_bytes_encrypted_after_regenerate` מאשרים ciphertext ב-DB (לא `%PDF`) ו-PDF תקין ב-Download.

---

## 8. אבטחת מידע והקשחה (Security Hardening) — עדיפות עליונה

> סימון: ✅ רץ בפועל · ⚠️ קיים חלקית · ❌ נדרש. מאומת מול הקוד ב-2026-08-02 (ראו סעיף 0).

| # | נושא | מצב | צ'ק ליסט |
|---|------|------|----------|
| S1 | הצפנה במנוחה ובתעבורה | ✅ | [x] AES-256 למנוחה — חלקי (SecureStorage/Keystore ב-Mobile) · ✅ **Mobile base URL = HTTPS** (`KtorClientFactory.kt:34`) — [x] מעבר ל-HTTPS + `networkSecurityConfig` + **Certificate Pinning** · [ ] אכיפת TLS 1.3 בלבד בשרת/פרוקסי · [ ] הצפנת DB/gibוי/קבצים AES-256 |
| S2 | MFA חובה (TOTP/WebAuthn/Passkeys) | ⚠️ | [x] Backend: `/auth/2fa/*` + `webauthn.py` רשומים; אתר: login-verify + settings/security + BiometricScanner · ❌ Mobile: אין MFA בכניסה (`AuthRepository.login`) — [ ] הוספת זרימת MFA ל-Mobile · [ ] הפיכת MFA **לחובה** (שליחים + אדמין), לא opt-in · [ ] access token 15 דק' (כיום 60) + refresh 7 ימים עם rotation (כיום 180) |
| S3 | API Security | ⚠️ | [x] Rate limiting — חלקי (auth/addresses/orders בלבד) — [ ] הרחבה גלובלית פר-משתמש/פר-IP · ❌ `security_middleware` (HMAC/Threat-Intel/Anomaly/Schema) = **קובץ מת** — [ ] להפעיל על routes או להסיר/לבנות מחדש · [x] `check_ip_block` פעיל ב-before_request · [x] CORS מדויק (`app.py:198`) · [ ] WAF בפרוקסי · [ ] OpenAPI + validation · [ ] סקר BOLA/IDOR |
| S4 | ניהול סודות | ⚠️ | ❌ מפתחות קשיחים בקוד: `dev-device-hmac-key` (api_security.py), fallback `dev-secret-key-change-in-production` (auth.py) — [ ] מעבר ל-Vault / AWS SM / Azure KV · [ ] רוטציה אוטומטית · [ ] אפס סודות בקונפיג/קוד |
| S5 | הגנת PII (חוק הפרטיות + GDPR) | ⚠️ | [x] `privacy.py` רשום ונבדק (`/api/privacy/export|account|consent`) · ❌ `data_rights.py` = קובץ מת (לא רשום) · ❌ Mobile: אין consent flow — [ ] הוספה · [ ] Data minimization · [ ] יומן גישה ל-PII · [ ] נוהל DPIA |
| S6 | אבטחת מובייל | ⚠️ | [x] `SecurityEnforcer` רץ ב-`MainActivity` (מינ. גרסה + Emulator + Hardware-backed) · [x] `SecureStorage`(AndroidKeyStore) + R8/Shrink + Play Integrity · ❌ `SecurityManager.kt` = קובץ מת · ❌ **אין Root/Jailbreak detection** — [ ] הוספה · [ ] Anti-tamper · [ ] Pinning (קישור S1) · [ ] הצפנת Room/Storage (`security-crypto`) |
| S7 | Pentest & Bug Bounty | ❌ | [ ] Pentest רבעוני · [ ] Bug Bounty · ❌ אין SAST/DAST מאומת ב-CI — [ ] Semgrep/SonarQube + OWASP ZAP · [ ] (קיימים tests אבטחה בסיסיים: headers/CORS/privacy) |
| S8 | Incident Response & Forensics | ⚠️ | [x] קיים JSON audit log (`logs/security.json`) המדמה SIEM · ❌ אין Playbook IR · [ ] SIEM אמיתי/לוגים מרכזיים · [ ] דיווח לרשויות תוך 72 שעות · [ ] פנקס אירועים |

## 9. ציות רגולטורי (Regulatory Compliance) — חובה לשוק ישראלי

| # | נושא | מצב | צ'ק ליסט |
|---|------|------|----------|
| R1 | חוק הגנת הפרטיות + תקנות אבטחת מידע (2017) | ⚠️ | [x] consent/חשבונות/מחיקה — זמין דרך `privacy.py` (רשום) · [ ] מיפוי מאגרי מידע · [ ] רישום ברשם מאגרי מידע · [ ] ממונה הגנת פרטיות (DPO) · [ ] נוהל אירוע אבטחה |
| R2 | נגישות (WCAG 2.1 AA) | ❌ | [ ] בדיקת ניגודיות באפליקציה ובאתר (כולל מצב יום החדש מסעיף 5) · [ ] ניווט מקלדת · [ ] Screen readers (TalkBack) · [ ] טפסים נגישים · [ ] בדיקה עם axe + ידני |
| R3 | דיני עבודה וזכויות שליחים | ⚠️ | [x] `hr_compliance.py` רשום + audit · [x] דו"ח 856 קיים ב-`reports.py` · [ ] סיווג נכון (עצמאי/שכיר) · [ ] תלוש שכר דיגיטלי תקני · [ ] **הפרשות פנסיה/ביטוח לאומי אוטומטיות (מתחבר לסעיף 1)** · [ ] דיווח 102/856 בזמן אמת |
| R4 | תקנות מס הכנסה (ניהול ספרים, חשבוניות דיגיטליות) | ⚠️ | [x] `invoices.py` — מספור רץ אטומי + VAT · [x] חתימה דיגיטלית (`digital_signature.py`, `docusign_service.py`) · [x] PDF/Word generators · [ ] ארכיון 7 שנים · [ ] שלמות שרשרת ביקורת |
| R5 | חוק הגנת הצרכן (ביטול עסקה, שקיפות מחירים) | ⚠️ | [x] מחיר כולל מע"מ + מעקב משלוח (tracking/pricing קיימים) · [ ] מדיניות ביטול ברורה · [ ] תיעוד הסכמה דיגיטלית |
| R6 | רגולציית תשלומים (PCI DSS) | ⚠️ | [x] ארנק/עסקאות פנימיים (`courier_wallet.py` + Mobile) · ❌ **PSP/Stripe חיצוני לא מאומת** (לוקר לפי PROJECT_STATUS) · [ ] אם סולקים ישירות — PCI DSS · [ ] טוקניזציה מלאה · [ ] ללא שמירת PAN |
| R7 | דרישות רשות המיסים / חופש מידע | ⚠️ | [x] דוחות: `vat-summary` + `annual-summary`(1301) + Excel ב-`reports.py` · [ ] API לדיווח אוטומטי (מע"מ מקוון, 102, 856) — מתחבר לסעיף 7 · [ ] חתימה אלקטרונית על דוחות · [ ] ממשק לדיווח |

## 10. אמינות, סקלביליות ותפעול (Reliability & Operations)

| # | נושא | מצב | צ'ק ליסט |
|---|------|------|----------|
| O1 | SLA & Observability | ⚠️ | [x] `apprunner.yaml` + JSON logs (`logs/security.json`) · [ ] זמינות 99.9%+ · [ ] OpenTelemetry · [ ] Prometheus/Grafana · [ ] ELK/Loki · [ ] Alerting מדורג |
| O2 | Chaos Engineering & DR | ⚠️ | [x] `docs/disaster_recovery_plan.md` + `backend/backups/` · [ ] RPO < 1 דק' / RTO < 15 דק' (לא מאומת) · [ ] גיבויים מוצפנים Off-site · [ ] בדיקת שחזור חודשית · [ ] Game Days |
| O3 | Feature Flags & Progressive Delivery | ❌ | [ ] LaunchDarkly/Unleash · [ ] Canary deployments · [ ] Rollback אוטומטי על שגיאות |
| O4 | API Versioning & Deprecation Policy | ❌ | [ ] Semantic versioning (כיום routes לא מנומספרות: `/api/...`) · [ ] 12 חודשים תמיכה בגרסאות קודמות · [ ] תיעוד שינויים פומבי |
| O5 | Performance Budgets | ⚠️ | [ ] Cold start < 2s · [ ] P95 API < 200ms · [ ] Bundle Android < 50MB (ShrinkResources מופעל ב-release) · [ ] Web Vitals (LCP < 2.5s, CLS < 0.1) · [ ] מדידות סוללה (מטרה: <10% ל-8 שעות) |

## 11. ממשל נתונים וביקורת (Data Governance & Audit)

| # | נושא | מצב | צ'ק ליסט |
|---|------|------|----------|
| G1 | Data Lineage & Catalog | ❌ | [ ] מיפוי כל זרימת נתונים (מקור → עיבוד → יעד) · [ ] סיווג רגישות (Public/Internal/Confidential/Restricted) |
| G2 | Audit Trail בלתי ניתן לשינוי | ⚠️ | [x] `log_audit` פעיל — DB (`AuditLog`) + JSON (`logs/security.json`), נקרא ב-25+ מקומות · [ ] שדרוג ל-Append-only log (WORM storage) · [ ] השלמת רישום: יצירת דוח מס, שינוי פרטים רגישים, תשלום |
| G3 | Data Retention & Disposal | ❌ | [ ] מחיקה אוטומטית: 7 שנים מס · [ ] 3 שנים לוגים אבטחה · [ ] Certificate of Destruction |
| G4 | Third-Party Risk Management | ⚠️ | [x] `docs/vendor_dpa_registry.md` קיים · [ ] הערכת ספקים (SOC 2 Type II, ISO 27001) · [ ] DPA חתומים · [ ] ניטור רציף |

## 12. ארכיטקטורה מאובטחת (Zero Trust)

```
                      Zero Trust Network
┌─────────────────────────────────────────────────────────────┐
│  Mobile App ──(mTLS)──► API Gateway (WAF, Rate Limit, Auth) │
│                          │                                   │
│           ┌──────────────┼──────────────┐                    │
│           ▼              ▼              ▼                    │
│      ┌─────────┐    ┌─────────┐    ┌─────────┐              │
│      │ Courier │    │ Business│    │  Docs   │  ...         │
│      │ Service │    │ Service │    │ Service │              │
│      └────┬────┘    └────┬────┘    └────┬────┘              │
│           │              │              │                    │
│           ▼              ▼              ▼                    │
│    ┌──────────────────────────────────────────┐             │
│    │        PostgreSQL (Encrypted, RLS)       │             │
│    │        + Audit Log (Immutable)           │             │
│    └──────────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────────┘
```

- **מצב קיים (מאומת):** RLS context מוזרק ב-`before_request` (Postgres) + script `enable_rls.py` · WebSocket רשום (`init_sockets`) · `apprunner.yaml` קיים · Rate limiting חלקי.
- **צ'ק ליסט:**
  - [ ] API Gateway / reverse proxy (WAF, Rate Limit, mTLS/TLS) לפני כל ה-services.
  - [ ] הפרדת services לוגית (Courier / Business / Docs / Payments) — גם אם בהתחלה תהליך אחד, לייצר גבולות אכיפה בקוד.
  - [ ] PostgreSQL עם RLS מופעל + הצפנת קולום רגיש.
  - [ ] Audit log Append-only (WORM) — מנותק מה-DB הראשי.

## Definition of Done — לכל פיצ'ר חדש

```
[ ] Threat Modeling (STRIDE) בוצע
[ ] Security Code Review עבר
[ ] SAST/DAST ב-CI עבר (0 Critical/High)
[ ] Penetration Test לפיצ'ר (אם משנה שטח תקיפה)
[ ] DPIA מלא אם מעבד PII
[ ] נגישות WCAG 2.1 AA נבדקה (axe + ידני)
[ ] Audit Trail מלא לפעולות רגישות
[ ] Data Retention מוגדר וממומש
[ ] תיעוד API מעודכן (OpenAPI)
[ ] Runbook תפעולי קיים
[ ] Load Test עבר (2x תעבורה צפויה)
[ ] Rollback נבדק בפועל
```

---

## סדר ביצוע מומלץ (תלותיות)

```
שלב 0+ — בלוקרים ראשוניים (קודם הכול, במקביל להכל)
  ✅ סעיף 13: העברת KAPT → KSP (תשתית Build יציבה) — הושלם 2026-08-03
  ✅ S1: מעבר ה-Mobile ל-HTTPS + Pinning ו-`networkSecurityConfig` (בוצע: KtorClientFactory.kt:34, network_security_config.xml, certs) — הושלם 2026-08-03
  S1-S8, R1-R7, G1-G4, O1-O2  ← בלוקרים: בלי זה לא עולים לפרודקשן

שלב A — ויזואלי ומהיר (1-2 ימים) — ✅ הושלם 2026-08-04
  #2 צבע מסלולים למותג ✅ (כבר על המותג)  →  #4 תואר שליח ✅ (בוצע)  →  #1 שורת פנסיה/השתלמות ✅ (כבר קיים מקצה לקצה)

שלב B — תכונות יומן (2-3 ימים)
  #3 יומן בלחיצת שעה (Backend POST → Model → UI) ✅ הושלם (כבר קיים מקצה לקצה)

שלב C — חוויית שימוש (2-3 ימים)
  #5 מצב יום/לילה אמיתי (Theme גלובלי + הסרת hardcoded) ← פתוח (גדול — שלב הבא)
  #6 תמונת פרופיל/לוגו ✅ הושלם (Photo Picker + העלאה + תצוגה)

שלב D — המערכת הגדולה (4-6 ימים)
  #7 טפסים ודוחות (3 סבבים, Backend-first)

שלב E — הקשחה תפעולית (שבוע 3-4)
  O3-O5, ארכיטקטורת Zero Trust, Chaos Engineering, DR Test
```

## הנחיות בדיקה ואימות

- **Backend:** `cd backend && pytest` (מערך קיים, כולל tests אבטחה/privacy/websocket/payments). לכל endpoint חדש — test + בדיקת BOLA/IDOR.
- **Android:** `./gradlew test`, build: `./gradlew assembleDebug` (מ-`mobile-native/courier-android`), בדיקה ידנית ב-Emulator (RTL + עברית + מצבי תאורה + TalkBack).
- **Frontend:** `npm run lint` + `npm run build` (מ-`frontend`).
- **אימות עברית/RTL:** לוודא שכל טקסט חדש נטען כ-UTF-8 (היו תקלות קידוד בעבר — לראות `fix_encoding.py`).
- **ביצועים:** מדידת סוללה/מהירות במצב יום ולילה (מטרה: <10% ב-8 שעות), Cold start < 2s, P95 API < 200ms.
- **אבטחה (DoD):** SAST/DAST ב-CI, סקר אבטחה לכל שינוי, בדיקת TLS 1.3 + Pinning, MFA חובה מול פרודקשן.

## קבצים מרכזיים (מפת שינוי)

| שכבה | קבצים |
|------|-------|
| Backend – פיצ'רים | `routes/courier_business.py`, `routes/courier_notifications.py`, `routes/freelance.py`, `routes/reports.py`, `routes/earnings_reports.py`, `utils/pdf_generator.py`, `utils/docx_generator.py`, `models.py` |
| Backend – אבטחה/ציות | `middleware/api_security.py`, `extensions.py` (limiter), `app.py` (CORS), `routes/auth.py` (MFA), `routes/webauthn.py`, `routes/consent.py`, `routes/data_rights.py`, `routes/privacy.py`, `utils/audit.py`, `utils/encryption.py`, `enable_rls.py`, `.github/workflows` (SAST/DAST) |
| App – UI | `ui/courier/BusinessScreen.kt`, `CalendarScreen.kt`, `SettingsScreen.kt`, `DocumentsScreen.kt`, `RoutePlannerScreen.kt`, `ManualRoutePlannerScreen.kt`, `MoreScreen.kt`, `ProfileScreen.kt` |
| App – Data | `model/BusinessModels.kt`, `model/User.kt`, `model/ScheduleDelivery*`, `network/DeliveryApi.kt`, `repository/BusinessRepository.kt`, `CalendarRepository.kt`, `CourierRepository.kt` |
| App – Theme | `ui/theme/Theme.kt`, `ui/theme/Color.kt`, `MainActivity.kt` |
| App – Security | `security/SecurityManager.kt` (AndroidKeyStore), `app/build.gradle.kts` (R8/Play Integrity), `network_security_config` (Pinning) |
| אתר | `frontend/app/courier/documents/page.tsx`, `frontend/app/courier/profile/page.tsx`, `frontend/app/courier/dashboard/page.tsx` |
| תפעול | `apprunner.yaml`, `docker-compose*.yml`, `infrastructure/`, `docs/` (Runbook, DR, Vendor DPA) |

---

## 13. תשתית Build — העברת KAPT → KSP (חובה ע"פ תוכנית החקירה)

> **מצב/מטרה:** המודול `courier-android` עושה שימוש ב-`kotlin-kapt` רק כדי להפעיל את שני ה-processors:
> **Room 2.7.1** (`androidx.room:room-compiler:2.7.1`) + **Hilt 2.56.2** (`libs.hilt.compiler`).
> Kapt מוגדר **deprecated** ב-Kotlin ונשבר ברצועת Kotlin 2.1.x / Gradle 8.14 לעיתים תכופות עם `e: Could not find module <Error module>` וקבצי codegen פגומים (נצפה בלוגי ה-Build). היעד: מעבר ל-**KSP** — נתמך רשמית גם על-ידי Room 2.7.x וגם על-ידי Hilt ≥2.52 (עם 2.56.2 זה drop-in). **שינוי מזערי: שלושה קבצים בלבד** (ללא מגע בקוד אפליקציה).
> 
> **סטטוס: ✅ הושלם (2026-08-03)** — Build מצליח, KSP מייצר DAO_Impl + Hilt components, אפס kapt.

### 13.1 מצב קיים — מפת נגיעות מדויקת (מאומת מול הקוד)
| פריט | קובץ | שורות/מנגנון |
|------|------|--------------|
| גרסת Kotlin | `gradle/libs.versions.toml` → `[versions] kotlin = "2.1.20"` | מכנה משותף |
| Plugin מיושן (kapt) | `courier-android/build.gradle.kts` → `id("kotlin-kapt")` | שורה 8 |
| Room processor | `courier-android/build.gradle.kts` → `kapt("androidx.room:room-compiler:2.7.1")` | שורה 87 |
| Hilt processor | `courier-android/build.gradle.kts` → `kapt(libs.hilt.compiler)` | שורה 91 |
| DB ראשי | `com.tzir.delivery.courier.database.TzirDatabase` (`@Database` v5, 8 `@Entity`, 46 `@Dao`+ops, 6 abstract DAOs) | ללא שינוי קוד |
| Hilt App | `com.tzir.delivery.courier.TzirCourierApp` (`@HiltAndroidApp`) + 10 markers (Module/InstallIn/AndroidEntryPoint) | ללא שינוי קוד |
| Room מצב schema | `TzirDatabase.kt` → `exportSchema = false` | ללא שינוי |

> **הערה חשובה:** המודול `customerApp` — **לא נוגעים בו כלל** (אין KAPT/Room/Hilt). זה הכל קורה אך ורק במודול `courierApp`. אם מתכסה הבנה ש"customer" זקוק ל- KSP — לא, נבדק.

### 13.2 שלבים מדויקים (קובץ-קובץ)

**שלב 1 — `mobile-native/gradle/libs.versions.toml`**
- בקטע `[versions]` הוסף שורה (זה מתאימה ל-Kotlin 2.1.20):
  ```toml
  ksp = "2.1.20-1.0.31"   # גרסת KSP תואמת ל-Kotlin 2.1.20 — בדוק/עדכן לגרסה האחרונה ≥1.0.31
  ```
- בקטע `[plugins]` הוסף:
  ```toml
  ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
  ```

**שלב 2 — `mobile-native/build.gradle.kts` (root)**
- בקטע `plugins` הוסף (לפי נוהל ה-`//trick` הקיים, להעברה אחידה לכל מודל):
  ```kotlin
  alias(libs.plugins.ksp) apply false
  ```

**שלב 3 — `mobile-native/courier-android/build.gradle.kts`**
- (א) בקטע `plugins` החלף את KAPT ב-KSP:
  ```kotlin
  // id("kotlin-kapt")          ← הסר
  alias(libs.plugins.ksp)       ← הוסף
  ```
- (ב) בקטע `dependencies` החלף את שני ה-kapt ב-ksp:
  ```kotlin
  ksp("androidx.room:room-compiler:2.7.1")
  ksp(libs.hilt.compiler)
  ```
  > הערה: כדי ליישר גרסאות טוב יותר, כדאי להוסיף `room-compiler` = `{ module = "androidx.room:room-compiler", version.ref = "room" }` ל-[libraries] ולכתוב `ksp(libs.room.compiler)` — אופציונלי.
- (ג) בקטע `kotlinOptions` אפשר אבל לא חובה להעביר ל-`compilerOptions` (best-practice ב-AGP 8.7) — איננה חלק מה-KSP.

**שלב 4 — `mobile-native/gradle.properties` (אופציונלי, מומלץ)**
- `kapt.incremental.apt=true` כבר לא רלוונטי אחרי KSP → מוטב להסירו (השארה לא מזיקה). הוסף:
  ```
  ksp.incremental=true
  ```

### 13.3 קבצים שלא נוגעים בהם בכלל
- קבצי `database/*` (Entities, DAOs, `TzirDatabase.kt`) — Room עובד זהה תחת KSP; **אין שינוי בקוד**.
- קבצי Hilt (`@HiltAndroidApp` / `@Module` / `@InstallIn` / `@AndroidEntryPoint`) — נתמכים ב-KSP; **אין שינוי**.
- `customer-android/**` — אין בו `kapt`; **לא נוגעים**.
- אין במודול שימוש ב-`kotlinx.serialization`/`@Parcelize` הדורש תצורה מיוחדת ל-KSP.

### 13.4 בדיקה ואימות
```powershell
# מתוך mobile-native — עם JDK 17+ (java חייב להיות ב-PATH)
.\gradlew.bat clean
.\gradlew.bat :courierApp:assembleDebug    # שם המודול הנכון (לא ':courier-android')
```
- ** בדיקת-איד:** ה-Build מצליח ומסתיים ב-`BUILD SUCCESSFUL`.
- **פלט־קוד שנוצר:** תחת `mobile-native/courier-android/build/generated/ksp/debug/kotlin/` (במקום נתיב `source/kapt/...`): `TzirDatabase_Impl.kt`, כל `*_Impl.kt` של ה-DAO, ומרכבי Hilt (`TzirCourierApp_HiltComponents.kt` וכו').
- **בדיקת runtime:** הרצת ה-App — פתיחת DB (Migration עד v5), הזרקת Hilt תקינה, פתיחת מסך עם `collectAsStateWithLifecycle`, ללא שגיאות Dagger.
- **בדיקת תלות:** `.\gradlew.bat :courierApp:dependencies --configuration debugCompileClasspath | findstr /i kapt` → לא אמור להחזיר שום תוצאה.

### 13.5 כשלים נפוצים
| תסמונת | סיבה | פתרון |
|--------|------|--------|
| `e: Could not load module <Error module>` | שייר קשור של kapt ב-`build/` | `.\gradlew.bat clean`; אם ממשיך — בדוק שאין `ksp.useK2=true` מנוגד, ונקה את `~/.gradle/caches` הרלוונטי |
| `Unresolved reference: Int/Boolean` בקבצי `db/*` | שייר קבצים ממודול `:shared`(SQLDelight) שהוסר | נקה את `build/` של `archive/shared` ושל `courier-android/build/generated`, ואז `clean` |
| כישלון Hilt (`@AndroidEntryPoint` לא מזוהה) | גרסת Hilt <2.52 | 2.56.2 הקיימת תומכת KSP — תקין |
| KSP איטי | ללא incremental | הוסף `ksp.incremental=true` ב-`gradle.properties` |

### 13.6 Rollback
- החזר את 3 השינויים: `id("kotlin-kapt")` (plugin) + `kapt(...)` (×2 ב-dependencies) + הסר את `ksp` מה-`libs.versions.toml`/root. ריצה `.\gradlew.bat clean` — חוזר למצב הקודם.

### 13.7 Definition of Done (KSP)
- [ ] `:courierApp:assembleDebug` מצליח אחרי `clean`
- [ ] הקובץ `TzirDatabase_Impl` מופק תחת `ksp/debug`
- [ ] Hilt: השקת האפליקציה ללא כשל DI
- [ ] בדיקת DAO על DB אמיתי (insert/query) באמולטור
- [ ] אפס קבצלי `kapt`/`kaptKotlin` בפלט (grep kapt שלילי)
- [ ] Rollback נבדק בפועל (13.6)

> **שווי גרסאות:** KSP חייב להתאמות ל-Kotlin (`2.1.20`). בעת שדרוג Kotlin — שדרגו KSP במקביל (פורמט `2.1.20-<ksp-release>`).
